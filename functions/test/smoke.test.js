const fs=require('fs'),path=require('path'),assert=require('assert'),test=require('node:test');
test('functions entry preserves ERP exports without external accounting connector',()=>{const s=fs.readFileSync(path.join(__dirname,'..','index.js'),'utf8');['applyInventoryMovement','caseEventLedger','createInventoryReservation','runAtpCheck','runMrp','setUserClaims','monitorCaseSla','createCreditRequest','submitCreditRequest','reviewCreditRequest','getCreditDocumentUrl'].forEach(x=>assert.match(s,new RegExp(x)));assert.doesNotMatch(s,/accountingConnector|externalErpWebhook|integrationOutbox/i);});
