document.addEventListener('DOMContentLoaded',()=>HR.init({init(data){document.getElementById('headChart').innerHTML=data.headcount.map(x=>`<div class="hr-bar-wrap"><div class="hr-bar" data-value="${x.value}" style="--h:${x.value/80*100}%"></div><div class="hr-bar-label">${x.label}</div></div>`).join('');const p=data.contract[0].value/74*100,p2=data.contract[1].value/74*100;document.getElementById('contractDonut').style.cssText=`--p:${p}%;--p2:${p2}%`;document.getElementById('contractLegend').innerHTML=data.contract.map(x=>`<div class="legend-row"><span class="legend-dot" style="background:${x.color}"></span><span>${x.label}: <strong>${x.value}</strong></span></div>`).join('');document.getElementById('absenceHeatmap').innerHTML='<div class="heat-cell"></div>'+['Ene','Feb','Mar','Abr','May','Jun','Jul','Ago','Sep','Oct','Nov','Dic'].map(x=>`<div class="heat-cell">${x}</div>`).join('')+data.absence.map(r=>`<div class="heat-cell"><strong>${r.office}</strong></div>`+r.values.map(v=>`<div class="heat-cell l${v}">${v}</div>`).join('')).join('');document.getElementById('indicatorList').innerHTML=data.indicators.map(i=>`<div class="hr-list-row"><div class="grow"><strong>${i.name}</strong><small>${i.value}% de ${i.target}%</small><div class="progress ${i.value>=i.target?'success':i.value/i.target<.8?'danger':'warning'}" style="margin-top:7px"><span style="width:${Math.min(100,i.value/i.target*100)}%"></span></div></div></div>`).join('');ERP.refreshIcons();}}));