document.addEventListener('DOMContentLoaded',()=>HR.init({init(data){let cases=HR.merge('ethic-cases',data.cases);document.getElementById('ethicSteps').innerHTML=data.steps.map((s,i)=>`<div class="workflow-step ${s.state}"><div class="step-circle">${i+1}</div><strong>${s.name}</strong><small>${s.detail}</small></div>`).join('');const render=()=>document.getElementById('caseBody').innerHTML=cases.map(c=>`<tr><td><strong>${c.id}</strong></td><td>${c.category}</td><td>${c.channel}</td><td>${ERP.formatDate(c.date)}</td><td>${c.owner}</td><td><span class="status ${HR.status(c.risk)}">${c.risk}</span></td><td><span class="status ${HR.status(c.status)}">${c.status}</span></td></tr>`).join('');render();HR.bindForm('ethicModalForm','ethic-cases',f=>({id:HR.uid('ETI'),category:f.get('category'),channel:f.get('channel'),"date":new Date().toISOString().slice(0,10),owner:'Comité de integridad',status:'Radicado',risk:'Por clasificar'}),x=>{cases.unshift(x);render()});ERP.refreshIcons();}}));