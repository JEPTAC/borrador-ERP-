document.addEventListener('DOMContentLoaded',()=>HR.init({init(data){document.getElementById('attendanceBody').innerHTML=data.attendance.map(a=>`<tr><td><div class="table-person"><div class="avatar">${ERP.initials(a.person)}</div><strong>${a.person}</strong></div></td><td>${a.office}</td><td>${a.entry}</td><td>${a.exit}</td><td>${a.worked}</td><td><div class="attendance-state"><span class="attendance-dot ${a.status==='Tarde'?'late':a.status==='Ausente'?'absent':''}"></span>${a.status}</div></td></tr>`).join('');document.getElementById('weeklyChart').innerHTML=data.weekly.map(x=>`<div class="hr-bar-wrap"><div class="hr-bar" data-value="${x.present}" style="--h:${x.present/74*100}%"></div><div class="hr-bar-label">${x.day}</div></div>`).join('');const update=()=>document.getElementById('clockNow').textContent=new Date().toLocaleTimeString('es-CO',{hour:'2-digit',minute:'2-digit',second:'2-digit'});update();setInterval(update,1000);let active=HR.load('clock',null);const renderClock=()=>{document.getElementById('clockState').textContent=active?`Entrada registrada a las ${active.time}`:'No tienes una marcación activa.';document.getElementById('clockBtn').innerHTML=active?'<i data-lucide="check"></i> Marcar salida':'<i data-lucide="check"></i> Marcar entrada';ERP.refreshIcons()};document.getElementById('clockBtn').onclick=()=>{if(active){ERP.toast('Salida registrada');active=null;localStorage.removeItem('erp-th-clock')}else{active={time:new Date().toLocaleTimeString('es-CO',{hour:'2-digit',minute:'2-digit'})};HR.save('clock',active);ERP.toast('Entrada registrada')}renderClock()};renderClock();}}));