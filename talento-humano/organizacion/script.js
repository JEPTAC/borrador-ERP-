document.addEventListener('DOMContentLoaded',()=>HR.init({init(data){let positions=HR.merge('positions',data.positions);document.getElementById('orgBranches').innerHTML=data.branches.map(b=>`<div class="org-branch"><div class="org-node"><strong>${b.name}</strong><small>${b.leader} · ${b.people} personas</small></div><div class="org-team">${b.teams.map(t=>`<div class="org-person">${t}</div>`).join('')}</div></div>`).join('');const render=()=>{document.getElementById('positionBody').innerHTML=positions.map(p=>`<tr><td><strong>${p.code}</strong></td><td>${p.name}</td><td>${p.level}</td><td>${p.office}</td><td><span class="status ${HR.status(p.status)}">${p.status}</span></td></tr>`).join('')};render();HR.bindForm('positionModalForm','positions',f=>Object.fromEntries(f.entries()),x=>{positions.unshift(x);render()});ERP.refreshIcons();}}));